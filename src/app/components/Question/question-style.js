const styles = theme => ({
    questionParagraph: {
        font: "-webkit-control",
        fontWeight: "bold",
        fontSize: "17px",
        marginTop: "20px",
        marginBottom: "0px",
        overflow: "auto"
    }
});

export default styles;
